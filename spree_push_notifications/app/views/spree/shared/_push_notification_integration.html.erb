<%# Spree Push Notifications Integration %>
<%# This partial loads the push notification JavaScript module which handles everything dynamically %>
<%# No HTML templates needed - the JavaScript creates all UI elements dynamically %>

<%# The push notification functionality is automatically loaded via importmap from: %>
<%# - spree/push_notifications.js (main logic, banner creation, permission handling) %>
<%# - /service-worker.js (service worker for receiving push notifications) %>

<%# Environment check to ensure proper configuration %>
<% if Rails.env.development? || ENV['VAPID_PUBLIC_KEY'].present? %>
  <%# All functionality is handled by the JavaScript module loaded via importmap %>
  <%# The banner and all interactions are created dynamically when needed %>
<% else %>
  <!-- Push notifications require VAPID keys to be configured -->
  <script>
    console.warn('Spree Push Notifications: VAPID keys not configured. Push notifications will not work.');
  </script>
<% end %>